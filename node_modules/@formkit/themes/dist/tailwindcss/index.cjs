'use strict';

var plugin = require('tailwindcss/plugin.js');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var plugin__default = /*#__PURE__*/_interopDefaultLegacy(plugin);

const outerAttributes = [
    'disabled',
    'invalid',
    'errors',
    'complete',
    'loading',
    'submitted',
    'multiple',
    'has-prefix-icon',
    'has-suffix-icon',
];
/**
 * The FormKit plugin for Tailwind
 * @public
 */
// @ts-expect-error matchVariant is not documented or have types
const FormKitVariants = plugin__default["default"](function ({ matchVariant }) {
    const attributes = outerAttributes.reduce((a, v) => ({ ...a, [v]: v }), {});
    matchVariant('formkit', (value = '', { modifier }) => {
        return modifier
            ? [
                `[data-${value}='true']:merge(.group\\/${modifier})&`,
                `[data-${value}='true']:merge(.group\\/${modifier}) &`,
            ]
            : [
                `[data-${value}='true']:not([data-type='repeater'])&`,
                `[data-${value}='true']:not([data-type='repeater']) &`,
            ];
    }, { values: attributes });
});

module.exports = FormKitVariants;
